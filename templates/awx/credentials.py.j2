DATABASES = {
    'default': {
        'ATOMIC_REQUESTS': True,
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': "{{ awx_postgres_database }}",
        'USER': "{{ awx_postgres_username }}",
        'PASSWORD': "{{ awx_postgres_password }}",
        'HOST': "{{ awx_postgres_hostname }}",
        'PORT': "{{ awx_postgres_port }}",
    }
}

BROADCAST_WEBSOCKET_SECRET = "{{ awx_broadcast_websocket_secret | b64encode }}"